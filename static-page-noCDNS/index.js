import React from "react";
import ReactDOM from "react-dom";

const root = ReactDOM.createRoot(document.getElementById("root"));

const navbar = (
  <nav>
    <h1> Reactino Nooberino</h1>
    <ul>
      <li>Pricing</li>
      <li>About</li>
      <li>Contact</li>
    </ul>
  </nav>
);

root.render(navbar);

// Reached lesson 9 (40:35)
